// Tombstone — formerly components/providers wrapper. Safe to delete.
"use client";
export function Providers({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
export default Providers;
